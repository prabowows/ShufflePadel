{
  "name": "shuffle-paddle",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "if [ ! -d \"_flutter\" ]; then git clone https://github.com/flutter/flutter.git --depth 1 -b stable _flutter; fi && ./_flutter/bin/flutter build web --release"
  }
}
